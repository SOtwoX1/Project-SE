import { Button } from "react-scroll";

export default function Login() {
    return (
        <div>
            <Button> hfdfjks </Button>

            <footer>Don't have an account? <Button >Register Now</Button> </footer>
        </div>
        
        
    );
}